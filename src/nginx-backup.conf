# Ajoute ce bloc dans /etc/nginx/sites-available/mobiglass
# à l'intérieur du bloc server {}, avant le location / {}

location /api/backup {
    proxy_pass http://localhost:3001/api/backup;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}
